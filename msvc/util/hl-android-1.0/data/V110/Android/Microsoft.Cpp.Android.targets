<!--


  *This file is automatically generated. Local changes risk being overwriten by the export process.*


-->

<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <!--
    Import targets for Visual Studio 2012/2013 support. Both use a 'Platform.targets' to an load appropriate toolset .target.
  -->

  <PropertyGroup>
    <RequiresPlatformDefinition Condition="Exists('$(VCTargetsPath)\Microsoft.Cpp.Platform.targets')">true</RequiresPlatformDefinition>
  </PropertyGroup>

  <Import Condition="'$(RequiresPlatformDefinition)' == 'true'" Project="$(VCTargetsPath)\Microsoft.Cpp.Platform.targets" />

  <!--
    Import targets for Visual Studio 2010 traditional support.
  -->

  <Import Condition="'$(RequiresPlatformDefinition)' != 'true' and Exists('$(VCTargetsPath)\Platforms\Android\ImportBefore')" Project="$(VCTargetsPath)\Platforms\Android\ImportBefore\*.targets" />
  <Import Condition="'$(RequiresPlatformDefinition)' != 'true'" Project="$(VCTargetsPath)\Platforms\Android\Android.Cpp.targets" />
  <Import Condition="'$(RequiresPlatformDefinition)' != 'true' and Exists('$(VCTargetsPath)\Platforms\Android\ImportAfter')" Project="$(VCTargetsPath)\Platforms\Android\ImportAfter\*.targets" />

</Project>

<!--

-->
